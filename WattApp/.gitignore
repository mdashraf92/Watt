# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
.artifacts/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files (private — never committed)
.env
.env*.local

# database dumps / backups (contain all user data — never commit)
*.sql.dump
gowatt_backup.sql
*_backup.sql
gowatt_public.sql
gowatt_auth_users.sql
gowatt_*.sql

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android
